# UI/UX Redefinition — Gas Québec

Design system generated by the `ui-ux-pro-max` skill (stack: react).
Direction: **Vibrant & Block-based** · Theme: **Light + Dark**.

## Design tokens
- Palette: "Event orange + map blue" — Primary `#EA580C`, Accent `#2563EB`, Success `#16A34A`
- Typography: Fira Sans (UI) + Fira Code (prices / tabular figures)
- Style keywords: bold, energetic, block layout, high contrast, animated hovers (200–300ms)

## Todo
- [x] Detect stack (React 18 + TS + Vite) and product type (fuel price finder)
- [x] Generate design system via skill `search.py`
- [x] Confirm direction with user (vibrant + dark mode)
- [x] `index.html` — fr lang, title, meta description, theme-color, Fira font links
- [x] `index.css` — semantic tokens (light + dark), base type, font
- [x] `App.css` — vibrant block restyle, header band, segmented view toggle, cards, dark mode
- [x] `App.tsx` — loading skeleton, styled error, header subtitle, theme toggle
- [x] `StationsTable.tsx` — responsive scroll wrapper, cheapest-price highlight, empty state, a11y
- [x] `SearchBar.tsx` — aria labels / token-based styling
- [x] `StationsMap.tsx` — token colors for markers/circle
- [x] Update README.md (design notes)
- [x] Verify: `tsc` + `npm run build` + `npm run lint` all green

## Review

### Changes made
- **`index.html`** — `lang="fr"`, real title + meta description + `theme-color`,
  preconnect + Fira Sans/Fira Code font links.
- **`src/index.css`** — full semantic token layer (light + dark), global focus
  ring, `prefers-reduced-motion` reset, Fira font stack.
- **`src/App.css`** — rewritten in the *Vibrant & Block-based* style: gradient
  header band, glassy theme toggle, card-blocked search/table, segmented pill
  view toggle, shimmering skeleton, styled error card, mono price cells with a
  green "Meilleur" badge, and responsive rules — all driven by tokens so dark
  mode is automatic.
- **`src/App.tsx`** — header band + subtitle, light/dark theme toggle
  (localStorage + OS default), skeleton loading state, `role="alert"` error,
  passes the selected gas type to the table for cheapest highlighting.
- **`src/StationsTable.tsx`** — dynamic columns, cheapest-price detection with
  badge, `overflow-x-auto` scroll wrapper, no-results empty state with
  suggestions, aria labels.
- **`src/SearchBar.tsx`** — aria labels on inputs/selects.
- **`src/StationsMap.tsx`** — marker/circle colors moved to a `MAP_COLORS`
  constant matching the palette (green = cheapest, orange = station, blue = radius).

### Verification
- `npx tsc --noEmit` → 0 errors
- `npm run build` → success (CSS 25.7 kB / 8.9 kB gzip)
- `npm run lint` → 0 warnings (`--max-warnings 0`)
- Skill pre-delivery checklist: contrast ≥4.5:1 tokens, visible focus rings,
  skeleton loading, reduced-motion honored, color never the sole indicator
  (badge + label), responsive table scroll.

### Notes / follow-ups
- Behavior is unchanged — this is purely presentational plus the theme toggle.
- The "Meilleur" badge marks the single cheapest row for the selected fuel in
  the current region filter; if that row is on another page its badge only shows
  once you page to it (after a radius search, results are price-sorted so it's
  row 1).
- Fonts load from Google Fonts (network); acceptable since the app already
  fetches remote station data.

## Iteration 2 — layout, location, itinerary, logos, autocomplete

- [x] **Split-view layout** — map (sticky on desktop) + results list side by
  side, stacked on mobile; replaced the Tableau/Carte toggle.
- [x] **Geolocation** — "Ma position" button (`navigator.geolocation`) with
  friendly permission/error messages (`geolocate()` in `geo.ts`).
- [x] **Itinerary routing** — "Sur mon trajet" mode: From → To geocoded, driving
  route from OSRM public API (`fetchRoute`), stations ranked within a corridor of
  the route (`filterAlongRoute`, point-to-segment distance + bbox pre-filter).
  Route + endpoints drawn on the map.
- [x] **Brand logos** — `BrandLogo` loads `/logos/<brand>.png` with a
  contrast-checked brand-colored initials badge fallback; shown in the list and
  map popups. `public/logos/README.md` documents the convention.
- [x] **Address autocomplete** — debounced, keyboard-navigable combobox
  (`AddressAutocomplete`) on all three address fields. Google Places when
  `VITE_GOOGLE_MAPS_API_KEY` is set (`googlePlaces.ts`, typed via
  `@types/google.maps`), else OpenStreetMap/Nominatim (`suggestAddresses`).
  Google selections resolve exact coordinates and skip the OSM geocode.

### Verification (iteration 2)
- `npx tsc --noEmit` → 0 errors · `npm run build` → success · `npm run lint` → 0 warnings
- API key stored only in `.env.local` (gitignored via `*.local`); never in source.

### Security note
- The pasted Google key is a browser key (exposed in the bundle by design).
  **Action for user:** restrict it by HTTP referrer + Places/Maps JS API in
  Google Cloud Console, and rotate it since it was shared in plaintext.

### Follow-ups / limits
- OSRM public demo server is rate-limited and not for production traffic; swap
  for a hosted OSRM/other routing provider if usage grows.
- The "Meilleur" badge marks the single cheapest visible row; on a non-current
  page it only shows once paged to (route/point results are price-sorted, so
  it's row 1).

## Iteration 3 — turn-by-turn live navigation

- [x] **Route engine** — `fetchRoute` now returns `RouteResult` (geometry +
  distance + duration + localized `steps`); French maneuver instructions built
  from OSRM maneuver type/modifier.
- [x] **Projection helpers** — `cumulativeKm` + `projectOnRoute` (cross-track +
  along-track distance); `filterAlongRoute` now attaches `alongKm` to each
  `RouteStation`.
- [x] **`useNavigation` hook** — `watchPosition` live tracking; computes remaining
  distance/ETA, current step + distance-to-maneuver, cheapest station still ahead,
  and off-route detection → automatic re-route (from current position to
  destination) with corridor recompute.
- [x] **`NavPanel`** — maneuver banner with SVG turn icons, "puis" preview,
  ETA/distance/price stats, best-station-ahead card, scrollable step list with
  current-step highlight, off-route/rerouting alerts, Arrêter button.
- [x] **Map** — live user marker + `FollowUser` recenters on GPS; `FitRoute`
  disabled while following.
- [x] **App** — `Démarrer la navigation` CTA on a computed route; NavPanel
  replaces the search bar while navigating; map/list driven by live nav data.

### Verification (iteration 3)
- `npx tsc --noEmit` → 0 · `npm run build` → success · `npm run lint` → 0 warnings.

### Limits
- Live GPS behaviour can't be exercised from a build check — needs a device/browser
  with location (and movement) to validate tracking, ETA drift and rerouting.
- No voice guidance (not available for a plain web app).
- Rerouting quality is bounded by the OSRM demo server.

## Iteration 4 — mobile-first + installable PWA (iPhone)

Goal clarified: install on the user's own iPhone (not the App Store), so → PWA
(Add to Home Screen), not Capacitor/native.

- [x] **Installable PWA** — hand-rolled after `vite-plugin-pwa` hit a
  `workbox-build` ESM error on Node 16. Static `public/manifest.webmanifest`,
  minimal `public/sw.js` (network-first navigation, cache-first assets),
  registered in `main.tsx`.
- [x] **App icons** — generated with `sharp` from an on-brand SVG
  (`scripts/gen-icons.mjs`): `pwa-192/512`, maskable-512, `apple-touch-icon` (180).
- [x] **iOS meta** — `apple-mobile-web-app-capable`, status-bar style, title,
  `apple-touch-icon`, `viewport-fit=cover`, manifest link.
- [x] **Mobile-first UI** — phone-locked container (max-width 520px + safe-area
  insets), single-column layout (dropped the desktop two-column split), 44px
  touch targets, 16px inputs (no iOS zoom), no tap-highlight, contained overscroll.
- [x] Documented the Netlify-Drop → Safari → Add-to-Home-Screen flow in README.

### Verification (iteration 4)
- `npx tsc --noEmit` → 0 · `npm run build` → success (manifest, sw.js, icons all
  emitted to `dist/`) · `npm run lint` → 0 warnings.

### Deploy note
- Build is static → any HTTPS static host works. GPS/geolocation requires HTTPS
  (secure context); plain-LAN http will block it on iOS.
- Google Places key is baked into the bundle at build time; add the deployed
  domain to the key's referrer allowlist or it falls back to OSM.

## Iteration 5 — route-only, map-first (Google-Maps-like)

Pivot: dropped "Autour de moi" (radius search) entirely; the app is now
route-centric with a full-screen map.

- [x] **Removed point mode** — no radius/geolocation-around-me search,
  `filterByRadius` no longer used in the UI; `SearchBar` is route-only (From → To,
  corridor, fuel). Deleted the unused `StationsTable` (react-table) component.
- [x] **Map-first shell** — full-screen `StationsMap` (zoom control hidden),
  floating search card + title/theme toggle on top, results in a **bottom sheet**
  (peek ↔ expanded via a grip handle) with a `StationList` of price-ranked cards
  (logo, brand, distance-to-route, price, "Meilleur").
- [x] **Navigation overlay** — during nav the sheet/search hide and `NavPanel`
  floats over the followed map.
- [x] Simplified `StationsMap` (removed center/radius circle + FlyTo).
- [x] Splash screen for load/error.

### Verification (iteration 5)
- `npx tsc --noEmit` → 0 · `npm run build` → success (bundle 115 → 99 kB gzip
  after dropping react-table) · `npm run lint` → 0 warnings.

## Iteration 6 — Google Maps base map + default Départ = current location

- [x] **Google Maps base map** — replaced Leaflet/OSM with the Google Maps JS API
  (`StationsMap` rewritten imperatively: `google.maps.Map`, circle markers with
  InfoWindows, route `Polyline`, endpoint + live-user markers, `fitBounds`,
  `panTo` follow, dark map style). Loader shared with Places via
  `loadGoogleMaps()`. Removed `react-leaflet`/`leaflet`/`@types/leaflet`
  (bundle 99 → 55 kB gzip).
- [x] **Default Départ = current location** — `SearchBar` geolocates on mount and
  reverse-geocodes (Google Geocoder when keyed, else Nominatim) to prefill the
  Départ field + store exact coords. `AddressAutocomplete` now only fetches
  suggestions on real user typing (`userTyped` ref) so the autofill doesn't pop a
  dropdown.
- [x] Google used for reverse geocoding when a key is present (no OSM in that path).

### Verification (iteration 6)
- `npx tsc --noEmit` → 0 · `npm run build` → success (JS 55 kB gzip) ·
  `npm run lint` → 0 warnings.

### Key requirements (Google Cloud Console)
- Enable **Maps JavaScript API** (map) + **Places API** (autocomplete) +
  **Geocoding API** (current-location/address resolve), billing on, and add
  `localhost` + deployed domain to the key's HTTP-referrer allowlist. Without a
  valid key the map shows a "carte indisponible" fallback.

## Iteration 7 — Google-Maps-style driving camera on navigation

- [x] **Navigation camera** — starting a route switches the map to a driving view:
  zoomed to street level, following the user with a **directional arrow** marker.
- [x] **3D tilt + heading-up rotation** when a **Vector Map ID** is provided
  (`VITE_GOOGLE_MAP_ID`) — `map.moveCamera({center, zoom:18, tilt:55, heading})`
  each GPS tick; resets tilt/heading to 0 on stop. Degrades to a zoomed north-up
  follow (arrow rotated by heading) when no Map ID.
- [x] **Heading + look-ahead** computed in `useNavigation`: `bearing()` of the
  route direction and a camera target ~50 m ahead on the route (`pointAtDistance`)
  so the road ahead is framed, not the car.
- [x] Env: `VITE_GOOGLE_MAP_ID` documented in `.env.example`, README, and
  `vite-env.d.ts`; dark map style skipped when a Map ID is used (styled in Cloud).

### Verification (iteration 7)
- `npx tsc --noEmit` → 0 · `npm run build` → success · `npm run lint` → 0 warnings.

## Iteration 8 — minimal nav panel + selectable destination station

- [x] **Slim nav panel** — `NavPanel` now shows only the current maneuver + the
  gas station ahead (with distance); removed the full step list, "puis" preview,
  and ETA/stats row.
- [x] **Select a station to navigate to** — tapping a `StationList` card selects it
  (`selectedKey`), computes a route Départ → station (`stationRoute`), redraws the
  map to it, and the CTA becomes "Y aller" → navigation targets that station.
  Falls back to the full Départ → Arrivée route when nothing is selected; selection
  clears on a new search.

### Verification (iteration 8)
- `npx tsc --noEmit` → 0 · `npm run build` → success · `npm run lint` → 0 warnings.

### Attribution note
- Declined to hide the Google logo / "Conditions d'utilisation" — required by
  Google Maps Platform ToS (hiding risks key suspension). Optional non-required
  UI already off via `disableDefaultUI`.

## Iteration 9 — form cleanup + faster/robust autocomplete + Places API (New)

- [x] **Removed** the corridor-distance and fuel-type selects from the search form
  (defaults: 2 km, Régulier). Form is now Départ / Arrivée / Trouver.
- [x] **Autocomplete responsiveness** — min 2 chars (was 3), 140 ms debounce
  (was 350), in-memory result cache; only calls OSM when Google yields nothing
  (fixed an empty-result bug that showed no suggestions).
- [x] **Places API (New)** — `AutocompleteSuggestion.fetchAutocompleteSuggestions`
  is now primary, with legacy `AutocompleteService` and then Nominatim as
  fallbacks; selection resolves coords via the new `Place.fetchFields`
  (legacy `getDetails` fallback). OSM `limit` raised 5 → 10.
- Note: Google autocomplete caps predictions at 5 with no pagination, so
  "load more" beyond 5 isn't possible on the Google path (OSM path returns ≤10).

### Verification (iteration 9)
- `npx tsc --noEmit` → 0 · `npm run build` → success · `npm run lint` → 0 warnings.
- Requires **Places API (New)** enabled on the key for the primary path.

## Iteration 10 — native iOS app (Capacitor + cloud build + AltStore)

Goal: install as a real app on the user's iPhone from Windows, free (no Mac,
no Apple Developer account). Chosen route: Capacitor → Codemagic cloud-Mac build
→ unsigned .ipa → AltStore sideload (free Apple ID, 7-day re-sign).

- [x] Added `@capacitor/core`, `@capacitor/ios`, `@capacitor/cli` (v5, Node-16 ok)
  and `capacitor.config.ts` (appId `com.essencequebec.app`, webDir `dist`).
- [x] `assets/icon.png` (1024) generated for `@capacitor/assets`; `ios/`/`android/`
  gitignored (regenerated in CI).
- [x] `codemagic.yaml` — cloud-Mac workflow: `npm ci` → `npm run build` (bakes
  `VITE_GOOGLE_*` from secure env) → `cap add ios` → icons → PlistBuddy adds
  `NSLocationWhenInUseUsageDescription` → `cap sync` → unsigned `xcodebuild archive`
  → zip `Payload/App.app` into `Essence.ipa`.
- [x] `DEPLOY-IPHONE.md` — full guide (GitHub push, Codemagic env + build, key
  referrer `capacitor://localhost`, AltServer/AltStore sideload, 7-day refresh,
  paid TestFlight alternative).

### Caveats to revisit
- WKWebView geolocation (iOS 15+) should work with the added permission string;
  fallback = `@capacitor/geolocation` plugin if live nav misbehaves.
- Google Maps JS key must allowlist `capacitor://localhost` or the map fails
  inside the app.

## Iteration 11 — live "my location" on the idle map

Problem: the current-location blue dot only appears during active turn-by-turn
navigation. When idle, `nav.userPosition` is `null`, so the map draws no marker
even though `StationsMap` already renders an idle dot (`circleIcon`) when given a
position and `follow` is false.

Plan:

A. Live "my location" dot (`src/App.tsx`, no changes to `StationsMap`):
- [x] Add `livePosition` state (`LatLng | null`).
- [x] Add a `useEffect` that calls `navigator.geolocation.watchPosition` on mount,
      updates `livePosition` on each reading, and `clearWatch` on unmount.
      Silently ignore errors (permission denied → stays `null`, no dot).
- [x] Pass `userPosition={nav.active ? nav.userPosition : livePosition}` to
      `StationsMap` so navigation still owns the position while active (camera
      follow/heading unchanged), and the idle map shows the live dot.

B. "Trouver" feedback + location-denied message (`src/SearchBar.tsx`):
- [x] Add a `notice` state shown below the form (reuse `.search-result-info`).
- [x] Empty submit → set "Entrez un point de départ et une arrivée." and return
      (instead of the current silent `return`).
- [x] On the initial `geolocate()` rejection, surface the localized error message
      (already produced by `geoErrorMessage`) so the blank Départ isn't a mystery.
- [x] Clear the notice when either field changes.

- [x] Verify `npx tsc --noEmit`, `npm run build`, `npm run lint` all clean.

### Review (iteration 11)
Changes made:
- `src/App.tsx` — added `livePosition` state + a mount-time `watchPosition` effect
  (cleaned up on unmount); the map now receives
  `userPosition={nav.active ? nav.userPosition : livePosition}`, so the live blue
  dot shows on the idle map and navigation still owns the position while active.
- `src/SearchBar.tsx` — added a `notice` line under the form: empty "Trouver"
  submit now says "Entrez un point de départ et une arrivée." instead of silently
  no-op'ing; a denied/failed initial geolocation surfaces its localized message;
  the notice clears as soon as either field changes.
- `README.md` — documented the live-location + denied-permission behavior.
- Verification: `tsc --noEmit` 0 errors · `eslint` 0 warnings · `vite build` OK.

Notes / trade-offs:
- The OS location prompt now appears on app open (idle GPS), not only on search/nav.
- Two `watchPosition` watchers can run briefly during navigation (idle + nav);
  harmless, but a future cleanup could pause the idle watch while `nav.active`.
- Still gated on a working `VITE_GOOGLE_MAPS_API_KEY`: no map ⇒ no dot to show.

## Iteration 12 — dismiss / hide the results sheet

Problem: after "Trouver", the expanded bottom sheet (`z-index: 700`, 68vh) covers
the top search card's "Effacer" button (`z-index: 600`) on shorter screens, so the
search can't be undone; the only collapse control was a tiny 40×5px grip.

Changes made:
- [x] `src/App.tsx` — added a **✕ close button** in the sheet summary row that
      calls `resetResults` (fully clears the search → plain map); made the summary
      text a `<button>` so tapping it collapses/expands the list (large target).
      The grip handle still toggles too.
- [x] `src/App.css` — button-reset styles for `.sheet-summary-text` (now `flex:1`)
      and a rounded `.sheet-close` control with hover state.
- [x] `README.md` — documented collapse-tap + ✕ clear.
- [x] Verified `tsc --noEmit` 0 · `eslint` 0 · `vite build` OK.

Note: kept z-index as-is; the sheet's own ✕ (z 700) is always reachable above the
search card, so raising the card wasn't needed and avoids hiding the sheet header.

## Iteration 13 — heads-up navigation (rotate map to GPS heading)

Ask: on starting an itinerary, focus on my position and rotate the map to my
heading (map turns as I turn).

Root cause of "it doesn't turn": rotation only runs in the `VECTOR` branch
(`StationsMap.tsx:232`, `moveCamera({heading})`), gated on `VITE_GOOGLE_MAP_ID`.
Without a Map ID the app uses a raster map, which Google can't rotate → north-up.
**Rotation requires a vector Map ID (config), not a code change.**

Code change (heading source — chosen: GPS course + route fallback):
- [x] `src/geo.ts` — new pure `resolveHeading(gpsHeading, speedMps, routeBearing)`:
      returns GPS `coords.heading` when moving (`speed > 1` m/s and heading valid),
      else the route bearing.
- [x] `src/useNavigation.ts` — `onPosition` now feeds
      `resolveHeading(pos.coords.heading, pos.coords.speed, routeBearing)` into
      `setHeading` (was route bearing only). Camera already centers on the user via
      the follow effect on the first GPS fix.
- [x] `README.md` — clarified that rotation needs a vector Map ID and documented
      the GPS-course heading.
- [x] Verified `tsc --noEmit` 0 · `eslint` 0 · `vite build` OK.

Required by the user (config, to actually see rotation):
- Create a **vector Map ID** and set `VITE_GOOGLE_MAP_ID` in `.env.local` + the
  Codemagic `google` group, then rebuild.

Not done (available if wanted): device-compass heading (turn while stationary,
needs iOS motion permission); seeding the first camera center from the idle
`livePosition` so the snap is instant instead of on the first nav GPS fix.

## Iteration 14 — iOS never asks for location (Capacitor Geolocation plugin)

Problem: on the wrapped iOS app the location permission prompt never appears.
Root cause: Capacitor's WKWebView does not surface HTML5 `navigator.geolocation`
prompts to iOS. The `@capacitor/geolocation` plugin (native `CLLocationManager`)
is required — it was not installed.

Changes made:
- [x] Added dependency `@capacitor/geolocation@^5`.
- [x] New `src/location.ts` — unified wrapper: native plugin when
      `Capacitor.isNativePlatform()`, else `navigator.geolocation`. Exposes
      `requestLocationPermission()`, `getCurrentPosition()`, and
      `watchPosition(onOk, onErr) → stop()`, normalizing lat/lng/heading/speed and
      localizing errors.
- [x] `src/geo.ts` — `geolocate()` now uses the wrapper (removed the direct
      `navigator.geolocation` call + `geoErrorMessage`).
- [x] `src/App.tsx` — idle "my location" watch uses the wrapper (this is what now
      triggers the iOS prompt on app open).
- [x] `src/useNavigation.ts` — nav watch uses the wrapper; numeric `watchId`
      replaced with a `stopWatch` cleanup function; removed `watchErrorMessage`.
- [x] `DEPLOY-IPHONE.md` — documented the plugin + no extra CI step.
- [x] Verified `tsc --noEmit` 0 · `eslint` 0 · `vite build` OK (plugin web impl is
      code-split into a small `web-*.js` chunk).

CI: no `codemagic.yaml` change needed — `npx cap sync ios` installs the plugin pod
and `NSLocationWhenInUseUsageDescription` is already added by the plist step.

Notes:
- No new marker code: idle branch at `StationsMap.tsx:212` already draws the dot
  and the camera only recenters when `follow` is true, so the map won't jump.
- Enabling GPS at rest means the OS location prompt now appears on app open.

### Verification (iteration 10)
- `npm run lint` → 0 · `npm run build` → success · `npx cap ls` runs (config valid).
- iOS archive itself only builds on the cloud Mac (can't be verified from Windows).
